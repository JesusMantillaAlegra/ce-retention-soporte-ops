# CE-Retention — Soporte Ops

**Este proyecto ya NO usa Next.js/Vercel.** Ver `INSTRUCTIVO.md` — sigue el mismo patrón que `CE-luciaBot`: `index.html` estático que carga datos desde `ce_retention_dashboard_data.js`/`.json`, regenerados por `sync_hubspot.mjs`.

Los siguientes archivos son restos del intento anterior (app Next.js/Vercel) y ya no se usan — se pueden borrar:
- `package.json`, `package-lock.json`, `next.config.js`, `.env.example`
- carpetas `app/` y `lib/`

(No los pude borrar yo mismo — el puente a tu equipo no tiene permiso de borrado — así que quedaron ahí hasta que los elimines a mano.)
